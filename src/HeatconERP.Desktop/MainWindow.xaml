<Window x:Class="HeatconERP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HeatconERP.Desktop"
        mc:Ignorable="d"
        Title="HeatconERP" Height="450" Width="700">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="HeatconERP - Manufacturing ERP" 
                   FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox x:Name="ApiUrlBox" Width="300" Padding="8,6" 
                     Text="http://localhost:5212" VerticalContentAlignment="Center"/>
            <Button x:Name="ConnectBtn" Content="Connect to API" 
                    Margin="10,0,0,0" Padding="15,8" Click="ConnectBtn_Click"/>
            <Button x:Name="GetUsersBtn" Content="Get Users" 
                    Margin="10,0,0,0" Padding="15,8" Click="GetUsersBtn_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#ccc" BorderThickness="1" CornerRadius="4" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="OutputText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
